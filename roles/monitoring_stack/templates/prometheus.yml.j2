global:
  scrape_interval: 15s

scrape_configs:
- job_name: 'node_exporter_metrics'
  static_configs:
  - targets:
{% for host in groups['all_server'] %}
    - '{{ hostvars[host]['ansible_host']}}:9100'
{% endfor %}
